function navigateToInitiative(initiative) {
    alert(`Learn more about ${initiative}!`);
    // Replace the alert with actual navigation if needed, like:
    // window.location.href = `${initiative}.html`;
}
